# 智能OA办公系统 - 源代码 + 注释完整工程

## 1. 项目源代码结构

### 1.1 整体目录结构

```
D:\sx1\
├── admin/                      # PC端管理后台 (Vue 3 + TypeScript + Vite)
│   ├── src/
│   │   ├── api/                # API接口定义
│   │   │   └── index.ts        # 统一API出口
│   │   ├── assets/             # 静态资源
│   │   ├── components/         # 组件库
│   │   │   ├── ApprovalDetail.vue      # 审批详情组件
│   │   │   ├── ApprovalManagement.vue  # 审批管理页面
│   │   │   ├── AttendanceDetail.vue    # 考勤详情组件
│   │   │   ├── AttendanceManagement.vue# 考勤管理页面
│   │   │   ├── Dashboard.vue           # 首页仪表盘
│   │   │   ├── GlobalLoading.vue       # 全局加载组件
│   │   │   ├── Login.vue               # 登录页面
│   │   │   ├── MeetingDetail.vue       # 会议详情组件
│   │   │   ├── MeetingManagement.vue   # 会议管理页面
│   │   │   ├── TaskDetail.vue          # 任务详情组件
│   │   │   ├── TaskManagement.vue      # 任务管理页面
│   │   │   └── ...                     # 其他业务组件
│   │   ├── store/              # 状态管理
│   │   │   ├── index.ts        # 主store入口
│   │   │   ├── types.ts        # 类型定义
│   │   │   ├── alert.ts        # 弹窗状态
│   │   │   ├── loading.ts      # 加载状态
│   │   │   └── actions/        # 业务操作
│   │   │       ├── employee.ts # 员工操作
│   │   │       ├── project.ts  # 项目操作
│   │   │       ├── task.ts     # 任务操作
│   │   │       └── ...         # 其他模块
│   │   ├── utils/              # 工具函数
│   │   │   ├── errorHandler.ts # 错误处理
│   │   │   ├── validator.ts    # 数据验证
│   │   │   ├── permission.ts   # 权限控制
│   │   │   ├── confirm.ts      # 确认对话框
│   │   │   └── retry.ts        # 请求重试
│   │   ├── App.vue             # 根组件
│   │   ├── main.ts             # 入口文件
│   │   └── style.css           # 全局样式
│   ├── package.json            # 依赖配置
│   └── vite.config.ts          # Vite配置
│
├── expo-domes/                 # 移动端App (React Native + Expo)
│   ├── src/
│   │   ├── api/                # API服务
│   │   │   ├── client.ts       # HTTP客户端
│   │   │   ├── services.ts     # 业务接口
│   │   │   └── session.ts      # 会话管理
│   │   ├── app/                # 页面组件
│   │   │   ├── index.tsx       # 登录页面
│   │   │   ├── workbench.tsx   # 工作台
│   │   │   ├── attendance.tsx  # 考勤打卡
│   │   │   ├── attendance-detail.tsx # 考勤详情
│   │   │   ├── approval.tsx    # 审批处理
│   │   │   └── ...             # 其他页面
│   │   ├── components/         # 公共组件
│   │   ├── utils/              # 工具函数
│   │   │   ├── errorHandler.ts # 错误处理
│   │   │   ├── validator.ts    # 数据验证
│   │   │   └── permission.ts   # 权限控制
│   │   └── config/             # 配置文件
│   │       └── constants.ts    # 常量定义
│   ├── package.json            # 依赖配置
│   └── app.json                # Expo配置
│
└── oa_server/                  # 后端API服务 (Node.js + Express + Prisma)
    ├── src/
    │   ├── routes/             # 路由控制器
    │   │   ├── auth.ts         # 认证路由
    │   │   ├── users.ts        # 用户路由
    │   │   ├── attendance.ts   # 考勤路由
    │   │   ├── attendanceDetail.ts # 考勤详情路由
    │   │   ├── approvals.ts    # 审批路由
    │   │   ├── tasks.ts        # 任务路由
    │   │   ├── meetings.ts     # 会议路由
    │   │   └── ...             # 其他路由
    │   ├── utils/              # 工具函数
    │   │   ├── swagger.ts      # API文档
    │   │   ├── logMiddleware.ts# 日志中间件
    │   │   └── onlineTracker.ts# 在线追踪
    │   └── index.ts            # 入口文件
    ├── prisma/
    │   └── schema.prisma       # 数据库模型定义
    ├── package.json            # 依赖配置
    └── .env                    # 环境变量
```

---

## 2. 核心模块注释说明

### 2.1 后端API (oa_server)

#### 2.1.1 认证模块 (auth.ts)

```typescript
/**
 * 认证路由模块
 * 处理用户登录、登出、权限查询等认证相关操作
 * 
 * @module routes/auth
 * @requires express
 * @requires prisma
 * @requires bcryptjs
 */

import { Router, Request, Response } from 'express';
import bcrypt from 'bcryptjs';
import { PrismaClient } from '@prisma/client';

const router = Router();
const prisma = new PrismaClient();

/**
 * 用户登录接口
 * @route POST /api/auth/login
 * @param {string} username - 用户名
 * @param {string} password - 密码
 * @returns {Object} 用户信息（不含密码）
 * 
 * @example
 * // 请求
 * POST /api/auth/login
 * { "username": "admin", "password": "123456" }
 * 
 * // 响应
 * { "code": 200, "data": { "id": 1, "username": "admin", ... }, "msg": "登录成功" }
 */
router.post('/login', async (req: Request, res: Response) => {
  try {
    const { username, password } = req.body;
    
    // 查询用户（包含部门和职位关联）
    const user = await prisma.sys_user.findFirst({
      where: { username, is_deleted: 0 },
      include: {
        sys_department: { select: { id: true, dept_name: true } },
        sys_position: { select: { id: true, pos_name: true } }
      }
    });
    
    // 用户不存在
    if (!user) return res.json({ code: 401, msg: '用户不存在' });
    
    // 验证密码（使用bcrypt加密比较）
    const valid = await bcrypt.compare(password, user.password);
    if (!valid) return res.json({ code: 401, msg: '密码错误' });
    
    // 检查账号状态
    if (user.status !== 1) return res.json({ code: 403, msg: '账号已被禁用' });
    
    // 返回用户信息（排除密码字段）
    const { password: _, ...userInfo } = user;
    res.json({ code: 200, data: userInfo, msg: '登录成功' });
  } catch (error) {
    res.status(500).json({ code: 500, error: String(error) });
  }
});

/**
 * 获取当前用户权限接口
 * @route GET /api/auth/permissions
 * @header {string} X-User-Id - 当前用户ID
 * @returns {Object} 用户角色和权限列表
 */
router.get('/permissions', async (req: Request, res: Response) => {
  // ... 权限查询逻辑
});

export default router;
```

#### 2.1.2 考勤模块 (attendance.ts)

```typescript
/**
 * 考勤管理路由模块
 * 处理员工打卡、考勤查询、设备绑定等操作
 * 
 * @module routes/attendance
 * @requires express
 * @requires prisma
 */

/**
 * 上班打卡接口
 * @route POST /api/attendance/clock-in
 * @param {number} user_id - 用户ID
 * @param {number} latitude - 纬度
 * @param {number} longitude - 经度
 * @param {string} device - 设备标识
 * @param {string} wifiName - WiFi名称
 * @param {string} wifiMac - WiFi MAC地址
 * @returns {Object} 打卡记录
 * 
 * @description
 * 打卡流程：
 * 1. 验证设备绑定（如开启设备校验）
 * 2. 验证地理位置（GPS围栏或WiFi验证）
 * 3. 计算考勤状态（正常/迟到）
 * 4. 创建或更新考勤记录
 */
router.post('/clock-in', async (req: Request, res: Response) => {
  try {
    const { user_id, latitude, longitude, device, wifiName, wifiMac } = req.body;
    const headerUserId = req.headers['x-user-id'] || req.headers['X-User-Id'];
    const userId = BigInt(user_id || headerUserId || 1);
    
    // 获取考勤配置
    const config = await getAttendanceConfig();
    const now = new Date();
    
    // 1. 设备验证（如开启）
    if (config.deviceCheck && device) {
      // ... 设备绑定验证逻辑
    }

    // 2. 地理围栏和WiFi验证
    let isWithinRange = false;
    let locationDesc = '';

    // 优先检查WiFi
    const hasWifiConfig = !!(config.wifiMac || config.wifiName);
    const clientHasWifi = !!(wifiMac || wifiName);
    if (hasWifiConfig && clientHasWifi) {
      // ... WiFi验证逻辑
    }

    // 其次检查GPS距离
    if (!isWithinRange && latitude !== undefined && longitude !== undefined) {
      const distance = getDistance(latitude, longitude, config.latitude, config.longitude);
      if (distance <= config.radius) {
        isWithinRange = true;
        locationDesc = `范围内打卡 (偏离公司 ${Math.round(distance)}米)`;
      }
    }

    // 3. 计算考勤状态
    let status = 'normal';
    const [startHour, startMin] = config.workTimeStart.split(':').map(Number);
    const targetDate = new Date(now);
    targetDate.setHours(startHour, startMin, 0, 0);
    targetDate.setMinutes(targetDate.getMinutes() + config.flexTime);
    
    // 超过弹性时间算迟到
    if (now.getTime() > targetDate.getTime()) {
      status = 'late';
    }

    // 4. 保存考勤记录
    // ... 创建或更新记录逻辑

    res.json({ code: 200, data: record, msg: '签到成功' });
  } catch (error) {
    res.status(500).json({ code: 500, error: String(error) });
  }
});

/**
 * 计算两点间距离（米）
 * 使用Haversine公式计算地球表面两点间的直线距离
 * 
 * @param {number} lat1 - 纬度1
 * @param {number} lon1 - 经度1
 * @param {number} lat2 - 纬度2
 * @param {number} lon2 - 经度2
 * @returns {number} 距离（米）
 */
function getDistance(lat1: number, lon1: number, lat2: number, lon2: number): number {
  const R = 6371e3; // 地球半径（米）
  const dLat = (lat2 - lat1) * Math.PI / 180;
  const dLon = (lon2 - lon1) * Math.PI / 180;
  const a =
    Math.sin(dLat / 2) * Math.sin(dLat / 2) +
    Math.cos(lat1 * Math.PI / 180) * Math.cos(lat2 * Math.PI / 180) *
    Math.sin(dLon / 2) * Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return R * c;
}
```

#### 2.1.3 考勤详情模块 (attendanceDetail.ts)

```typescript
/**
 * 考勤详情路由模块
 * 处理考勤详情查询、工资影响计算、申诉管理等
 * 
 * @module routes/attendanceDetail
 * @requires express
 * @requires prisma
 */

/**
 * 获取考勤详情接口
 * @route GET /api/attendance-detail/detail/:id
 * @param {string} id - 考勤记录ID
 * @returns {Object} 考勤详情（包含处罚和申诉信息）
 * 
 * @description
 * 返回数据包含：
 * - 基本考勤信息（打卡时间、地点、状态）
 * - 员工信息（姓名、部门、职位）
 * - 处罚记录列表
 * - 申诉记录列表
 * - 工资影响计算结果
 */
router.get('/detail/:id', async (req: Request, res: Response) => {
  try {
    const { id } = req.params;

    // 查询考勤记录（包含关联的处罚和申诉）
    const attendance = await prisma.oa_attendance.findUnique({
      where: { id: BigInt(id) },
      include: {
        oa_attendance_penalty: true,
        oa_attendance_appeal: true
      }
    });

    // ... 数据处理和工资影响计算逻辑

    res.json({ code: 200, data: detail, msg: 'success' });
  } catch (error) {
    res.status(500).json({ code: 500, error: String(error) });
  }
});

/**
 * 提交考勤申诉接口
 * @route POST /api/attendance-detail/appeal
 * @param {string} attendance_id - 考勤记录ID
 * @param {string} appeal_type - 申诉类型（late/early/absent）
 * @param {string} reason - 申诉原因
 * @returns {Object} 申诉记录
 */
router.post('/appeal', async (req: Request, res: Response) => {
  // ... 申诉创建逻辑
});
```

---

### 2.2 PC端 (admin)

#### 2.2.1 API接口定义 (api/index.ts)

```typescript
/**
 * API接口统一定义模块
 * 所有后端API请求在此统一定义和导出
 * 
 * @module api
 * @requires axios
 */

import axios from 'axios';

/**
 * 创建axios实例
 * @description
 * - baseURL: API基础路径，通过Vite代理转发到后端
 * - timeout: 请求超时时间（10秒）
 */
const api = axios.create({
  baseURL: '/api',
  timeout: 10000
});

/**
 * 请求拦截器
 * @description
 * 自动在请求头中携带当前用户信息：
 * - X-User-Id: 用户ID
 * - X-User-Name: 用户名
 * - X-Real-Name: 真实姓名（URL编码）
 */
api.interceptors.request.use(
  config => {
    const userJson = localStorage.getItem('currentUser');
    if (userJson) {
      const user = JSON.parse(userJson);
      config.headers['X-User-Id'] = String(user.id || '');
      config.headers['X-User-Name'] = user.username || '';
      config.headers['X-Real-Name'] = encodeURIComponent(user.real_name || '');
    }
    return config;
  },
  error => Promise.reject(error)
);

/**
 * 响应拦截器
 * @description
 * 统一处理响应数据：
 * - code === 200: 返回data数据
 * - 其他code: 抛出错误
 */
api.interceptors.response.use(
  response => {
    const data = response.data;
    if (data.code === 200) {
      return data.data;
    }
    return Promise.reject(new Error(data.msg || data.error || '请求失败'));
  },
  error => Promise.reject(error)
);

// ========== 考勤相关接口 ==========

/**
 * 获取考勤记录列表
 * @param {Object} params - 查询参数
 * @param {number} params.user_id - 用户ID（可选）
 * @param {string} params.date - 日期（可选）
 * @returns {Promise<Array>} 考勤记录列表
 */
export const getAttendance = (params?: { user_id?: number; date?: string }) =>
  api.get('/attendance', { params });

/**
 * 获取考勤详情
 * @param {string|number} id - 考勤记录ID
 * @returns {Promise<Object>} 考勤详情（包含处罚和申诉）
 */
export const getAttendanceDetail = (id: string | number) =>
  api.get(`/attendance-detail/detail/${id}`);

/**
 * 获取考勤汇总（首页和我的页面使用）
 * @returns {Promise<Object>} 考勤汇总数据
 */
export const getAttendanceSummary = () =>
  api.get('/attendance-detail/summary');

/**
 * 提交考勤申诉
 * @param {Object} data - 申诉数据
 * @param {string|number} data.attendance_id - 考勤记录ID
 * @param {string} data.appeal_type - 申诉类型
 * @param {string} data.reason - 申诉原因
 * @returns {Promise<Object>} 申诉记录
 */
export const submitAttendanceAppeal = (data: {
  attendance_id: string | number;
  appeal_type: string;
  reason: string;
}) => api.post('/attendance-detail/appeal', data);

// ========== 审批相关接口 ==========

/**
 * 获取审批列表
 * @param {Object} params - 查询参数
 * @param {string} params.status - 状态筛选
 * @returns {Promise<Array>} 审批列表
 */
export const getApprovals = (params?: { status?: string }) =>
  api.get('/approvals', { params });

/**
 * 通过审批
 * @param {string|number} id - 审批ID
 * @returns {Promise<Object>} 更新后的审批记录
 */
export const approveApproval = (id: string | number) =>
  api.put(`/approvals/${id}/approve`);

/**
 * 拒绝审批
 * @param {string|number} id - 审批ID
 * @param {string} reason - 拒绝原因
 * @returns {Promise<Object>} 更新后的审批记录
 */
export const rejectApproval = (id: string | number, reason?: string) =>
  api.put(`/approvals/${id}/reject`, { reason });

// ... 其他接口定义
```

#### 2.2.2 状态管理 (store/index.ts)

```typescript
/**
 * 全局状态管理模块
 * 使用Vue 3的reactive实现全局状态管理
 * 
 * @module store
 * @requires vue
 * @requires api
 */

import { reactive } from 'vue';
import * as api from '../api';

/**
 * 员工接口定义
 * @interface Employee
 * @property {string} id - 工号
 * @property {string} name - 姓名
 * @property {string} gender - 性别
 * @property {string} dept - 部门
 * @property {string} role - 角色/职位
 * @property {string} phone - 手机号
 * @property {string} status - 账号状态（正常/禁用）
 * @property {string} hrStatus - 人事状态（在职/试用期/离职）
 */
export interface Employee {
  id: string;
  name: string;
  gender: string;
  dept: string;
  role: string;
  position: string;
  phone: string;
  status: string;
  hrStatus: string;
  createTime: string;
  joinDate: string;
  contractEnd: string;
  userId?: number;
}

/**
 * 全局响应式状态
 * @description
 * 包含系统所有业务数据：
 * - employees: 员工列表
 * - attendance: 考勤记录
 * - approvals: 审批列表
 * - tasks: 任务列表
 * - meetings: 会议列表
 * - userPermissions: 当前用户权限
 */
export const store = reactive({
  employees: [] as Employee[],
  attendance: [] as Attendance[],
  approvals: [] as ApprovalItem[],
  tasks: [] as TaskItem[],
  meetings: [] as MeetingItem[],
  // ... 其他状态
  userPermissions: {
    roleCode: '',
    roleName: '',
    permissions: [] as string[]
  }
});

/**
 * 从API加载所有数据
 * @description
 * 并行加载所有业务数据，包括：
 * - 用户/员工数据
 * - 考勤记录
 * - 审批列表
 * - 任务列表
 * - 会议列表
 * - 系统配置
 * - 用户权限
 * 
 * 使用Promise.allSettled确保部分接口失败不影响其他数据加载
 */
export const loadAllData = async () => {
  try {
    const [
      usersData,
      attendanceData,
      approvalsData,
      tasksData,
      meetingsData,
      permsData,
      // ... 其他数据
    ] = await Promise.allSettled([
      api.getUsers(),
      api.getAttendance(),
      api.getApprovals(),
      api.getTasks(),
      api.getMeetings(),
      api.getCurrentPermissions(),
      // ... 其他请求
    ]);

    // 处理用户数据
    if (usersData.status === 'fulfilled') {
      store.employees = (usersData.value as any).map((u: any) => ({
        id: u.emp_no || String(u.id),
        name: u.real_name,
        gender: u.gender === 1 ? '男' : '女',
        dept: u.sys_department?.dept_name || '',
        role: u.sys_position?.pos_name || '',
        // ... 其他字段映射
      }));
    }

    // 处理考勤数据
    if (attendanceData.status === 'fulfilled') {
      store.attendance = (attendanceData.value as any).map((a: any) => ({
        date: formatDate(a.attend_date),
        empId: String(a.user_id),
        clockIn: formatTime(a.clock_in_time),
        clockOut: formatTime(a.clock_out_time),
        workHours: a.work_hours ? `${a.work_hours}h` : '--',
        status: mapAttendanceStatus(a.status),
        // ... 其他字段映射
      }));
    }

    // ... 其他数据处理

  } catch (error) {
    console.error('加载数据失败:', error);
  }
};
```

#### 2.2.3 工具函数 (utils/)

**错误处理 (errorHandler.ts)**

```typescript
/**
 * 全局错误处理模块
 * 统一处理和记录应用错误
 * 
 * @module utils/errorHandler
 */

export interface AppError {
  code?: number;
  message: string;
  details?: any;
  timestamp?: Date;
}

/**
 * 错误处理器类
 * @description
 * 功能：
 * - 错误标准化：统一错误格式
 * - 错误日志：记录错误历史
 * - 用户通知：根据错误类型显示友好提示
 */
class ErrorHandler {
  private errorLog: AppError[] = [];
  private maxLogSize = 100;

  /**
   * 处理错误
   * @param {any} error - 原始错误对象
   * @param {string} context - 错误上下文
   * @returns {AppError} 标准化错误对象
   */
  handle(error: any, context?: string): AppError {
    const appError = this.normalize(error, context);
    this.log(appError);
    this.notify(appError);
    return appError;
  }

  /**
   * 标准化错误
   * @private
   */
  private normalize(error: any, context?: string): AppError {
    let message = '未知错误';
    let code = 500;

    if (typeof error === 'string') {
      message = error;
    } else if (error?.response?.data) {
      const data = error.response.data;
      message = data.message || data.msg || '请求失败';
      code = data.code || error.response.status || 500;
    } else if (error?.message) {
      message = error.message;
    }

    if (context) {
      message = `[${context}] ${message}`;
    }

    return { code, message, details: error, timestamp: new Date() };
  }

  /**
   * 显示错误通知
   * @private
   */
  private notify(error: AppError) {
    if (error.code === 401) {
      Alert.alert('登录过期', '请重新登录');
    } else if (error.code === 403) {
      Alert.alert('权限不足', '您没有权限执行此操作');
    } else if (error.code === 404) {
      Alert.alert('未找到', '请求的资源不存在');
    } else if (error.code && error.code >= 500) {
      Alert.alert('服务器错误', '服务器内部错误，请稍后再试');
    } else {
      Alert.alert('操作失败', error.message);
    }
  }
}

export const errorHandler = new ErrorHandler();
```

**数据验证 (validator.ts)**

```typescript
/**
 * 数据验证模块
 * 提供表单数据验证功能
 * 
 * @module utils/validator
 */

export interface ValidationRule {
  required?: boolean;
  minLength?: number;
  maxLength?: number;
  pattern?: RegExp;
  message: string;
}

/**
 * 验证器对象
 * @description
 * 提供以下验证功能：
 * - 单字段验证
 * - 多字段批量验证
 * - 常用验证规则库
 */
export const Validator = {
  /**
   * 验证单个字段
   * @param {any} value - 待验证的值
   * @param {ValidationRule[]} rules - 验证规则数组
   * @returns {ValidationResult} 验证结果
   */
  validate(value: any, rules: ValidationRule[]): ValidationResult {
    const errors: string[] = [];

    for (const rule of rules) {
      if (rule.required && this.isEmpty(value)) {
        errors.push(rule.message);
        continue;
      }
      if (rule.minLength !== undefined && typeof value === 'string' && value.length < rule.minLength) {
        errors.push(rule.message);
      }
      if (rule.pattern && typeof value === 'string' && !rule.pattern.test(value)) {
        errors.push(rule.message);
      }
    }

    return { valid: errors.length === 0, errors };
  },

  /**
   * 判断值是否为空
   * @private
   */
  isEmpty(value: any): boolean {
    if (value === null || value === undefined) return true;
    if (typeof value === 'string') return value.trim() === '';
    if (Array.isArray(value)) return value.length === 0;
    return false;
  }
};

/**
 * 常用验证规则
 * @description
 * 预定义的验证规则，可直接使用
 */
export const CommonRules = {
  /** 必填验证 */
  required: (message = '此项为必填项'): ValidationRule => ({
    required: true,
    message
  }),

  /** 手机号验证 */
  phone: (message = '请输入正确的手机号'): ValidationRule => ({
    pattern: /^1[3-9]\d{9}$/,
    message
  }),

  /** 邮箱验证 */
  email: (message = '请输入正确的邮箱地址'): ValidationRule => ({
    pattern: /^[^\s@]+@[^\s@]+\.[^\s@]+$/,
    message
  })
};
```

**权限控制 (permission.ts)**

```typescript
/**
 * 权限控制模块
 * 提供基于角色的权限检查功能
 * 
 * @module utils/permission
 * @requires store
 */

import { store } from '../store';

/**
 * 权限管理对象
 * @description
 * 功能：
 * - 权限检查：判断用户是否拥有指定权限
 * - 权限要求：不满足权限时抛出错误
 * - 权限指令：Vue自定义指令v-permission
 */
export const Permission = {
  /**
   * 检查是否有权限
   * @param {string|string[]} code - 权限码
   * @param {Object} options - 选项
   * @param {string} options.mode - 检查模式（'all' | 'some'）
   * @returns {boolean} 是否有权限
   */
  has(code: string | string[], options: { mode?: 'all' | 'some' } = {}): boolean {
    const { mode = 'some' } = options;

    // 管理员拥有所有权限
    if (store.userPermissions.roleCode === 'ROLE_ADMIN') {
      return true;
    }

    const codes = Array.isArray(code) ? code : [code];
    const userPermissions = store.userPermissions.permissions;

    if (mode === 'all') {
      return codes.every(c => userPermissions.includes(c));
    }
    return codes.some(c => userPermissions.includes(c));
  },

  /**
   * 要求必须有权限
   * @param {string|string[]} code - 权限码
   * @param {string} action - 操作描述
   * @throws {Error} 权限不足时抛出错误
   */
  require(code: string | string[], action?: string): void {
    if (!this.has(code)) {
      const actionText = action || '执行此操作';
      throw new Error(`权限不足：您没有权限${actionText}`);
    }
  }
};

/**
 * 权限指令
 * @description
 * 用法：v-permission="'org:user'" 或 v-permission="['org:user', 'org:dept']"
 */
export const permissionDirective = {
  mounted(el: HTMLElement, binding: any) {
    const { value } = binding;
    if (!Permission.has(value)) {
      el.parentNode?.removeChild(el);
    }
  }
};
```

---

### 2.3 移动端 (expo-domes)

#### 2.3.1 HTTP客户端 (api/client.ts)

```typescript
/**
 * HTTP客户端模块
 * 封装fetch API，提供统一的网络请求接口
 * 
 * @module api/client
 * @requires react-native
 * @requires expo-constants
 */

import { Platform } from 'react-native';
import Constants from 'expo-constants';
import { getCurrentUser } from './session';

/**
 * 获取API基础地址
 * @description
 * 根据运行环境自动选择API地址：
 * - Web: 使用当前hostname
 * - Android模拟器: 10.0.2.2
 * - iOS模拟器: localhost
 * - 真机: 使用Expo hostUri
 */
const getBaseUrl = () => {
  if (Platform.OS === 'web') {
    const hostname = typeof window !== 'undefined' ? window.location.hostname : 'localhost';
    return `http://${hostname}:3000/api`;
  }

  const hostUri = Constants.expoConfig?.hostUri;
  if (hostUri) {
    const host = (hostUri as string).split(':')[0];
    if (host) {
      return `http://${host}:3000/api`;
    }
  }

  return Platform.OS === 'android'
    ? 'http://10.0.2.2:3000/api'
    : 'http://localhost:3000/api';
};

/**
 * 获取请求头
 * @param {boolean} isGet - 是否为GET请求
 * @returns {Object} 请求头对象
 * @description
 * 自动携带当前用户信息：
 * - x-user-id: 用户ID
 * - x-user-name: 用户名
 */
const getHeaders = (isGet = false) => {
  const headers: Record<string, string> = {};
  if (!isGet) {
    headers['Content-Type'] = 'application/json';
  }
  const currentUser = getCurrentUser();
  if (currentUser?.id) {
    headers['x-user-id'] = String(currentUser.id);
  }
  if (currentUser?.username) {
    headers['x-user-name'] = String(currentUser.username);
  }
  return headers;
};

/**
 * 带重试的请求包装
 * @param {Function} fn - 请求函数
 * @param {Object} options - 重试选项
 * @returns {Promise<T>} 请求结果
 */
const withRetry = async <T>(fn: () => Promise<T>, options = {}) => {
  const { maxRetries = 3, delay = 1000, backoff = 2 } = options;
  let lastError: any;

  for (let attempt = 0; attempt <= maxRetries; attempt++) {
    try {
      return await fn();
    } catch (error) {
      lastError = error;
      if (attempt < maxRetries) {
        const waitTime = delay * Math.pow(backoff, attempt);
        await new Promise(resolve => setTimeout(resolve, waitTime));
      }
    }
  }
  throw lastError;
};

/**
 * GET请求
 * @template T - 响应数据类型
 * @param {string} path - 请求路径
 * @param {Object} params - 查询参数
 * @returns {Promise<T>} 响应数据
 */
export const apiGet = async <T = any>(path: string, params?: Record<string, string | number>): Promise<T> => {
  let url = `${BASE_URL}${path}`;
  if (params) {
    const searchParams = new URLSearchParams();
    Object.entries(params).forEach(([key, value]) => {
      if (value !== undefined && value !== null) {
        searchParams.append(key, String(value));
      }
    });
    const qs = searchParams.toString();
    if (qs) url += `?${qs}`;
  }

  return withRetry(async () => {
    const response = await fetch(url, { headers: getHeaders(true) });
    const data = await response.json();
    if (data.code === 200) return data.data;
    throw { code: data.code, message: data.msg || '请求失败' };
  });
};

/**
 * POST请求
 * @template T - 响应数据类型
 * @param {string} path - 请求路径
 * @param {any} body - 请求体
 * @returns {Promise<T>} 响应数据
 */
export const apiPost = async <T = any>(path: string, body?: any): Promise<T> => {
  return withRetry(async () => {
    const response = await fetch(`${BASE_URL}${path}`, {
      method: 'POST',
      headers: getHeaders(),
      body: JSON.stringify(body)
    });
    const data = await response.json();
    if (data.code === 200) return data.data;
    throw { code: data.code, message: data.msg || '请求失败' };
  });
};
```

#### 2.3.2 考勤详情页面 (app/attendance-detail.tsx)

```typescript
/**
 * 考勤详情页面
 * 展示考勤详细信息、工资影响、处罚记录、申诉功能
 * 
 * @module app/attendance-detail
 * @requires react
 * @requires expo-router
 * @requires react-native
 */

import React, { useState, useEffect } from 'react';
import { View, Text, StyleSheet, ScrollView, TouchableOpacity, Alert, TextInput, Modal } from 'react-native';
import { useRouter, useLocalSearchParams } from 'expo-router';
import { Feather } from '@expo/vector-icons';
import { fetchAttendanceDetail, submitAttendanceAppeal } from '../api/services';

/**
 * 考勤详情页面组件
 * @description
 * 功能：
 * - 显示考勤基本信息（打卡时间、地点、状态）
 * - 显示工资影响（加班费、处罚金额）
 * - 显示处罚记录列表
 * - 显示申诉记录列表
 * - 提交考勤申诉
 */
export default function AttendanceDetailScreen() {
  const router = useRouter();
  const { id } = useLocalSearchParams();
  const [loading, setLoading] = useState(true);
  const [detail, setDetail] = useState<any>(null);
  const [showAppealModal, setShowAppealModal] = useState(false);
  const [appealType, setAppealType] = useState('late');
  const [appealReason, setAppealReason] = useState('');

  /**
   * 加载考勤详情数据
   */
  useEffect(() => {
    loadDetail();
  }, [id]);

  const loadDetail = async () => {
    setLoading(true);
    try {
      const res = await fetchAttendanceDetail(id as string);
      setDetail(res);
    } catch (error: any) {
      Alert.alert('错误', error.message || '加载考勤详情失败');
    } finally {
      setLoading(false);
    }
  };

  /**
   * 提交考勤申诉
   * @description
   * 验证表单数据后提交申诉，成功后刷新页面数据
   */
  const handleSubmitAppeal = async () => {
    if (!appealReason.trim()) {
      Alert.alert('提示', '请填写申诉原因');
      return;
    }

    try {
      await submitAttendanceAppeal({
        attendance_id: id as string,
        appeal_type: appealType,
        reason: appealReason
      });
      Alert.alert('成功', '申诉提交成功，等待审批');
      setShowAppealModal(false);
      setAppealReason('');
      loadDetail(); // 刷新数据
    } catch (error: any) {
      Alert.alert('错误', error.message || '申诉提交失败');
    }
  };

  // ... 渲染逻辑
}
```

---

## 3. 代码规范

### 3.1 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 文件名 | PascalCase（组件）/ camelCase（工具） | `UserManagement.vue` / `errorHandler.ts` |
| 变量名 | camelCase | `userName`, `isLoading` |
| 常量名 | UPPER_SNAKE_CASE | `API_BASE_URL`, `MAX_RETRY_COUNT` |
| 函数名 | camelCase | `getUserList()`, `handleSubmit()` |
| 接口名 | PascalCase + I前缀（可选） | `Employee`, `ApiResponse` |
| CSS类名 | kebab-case | `.user-card`, `.btn-primary` |

### 3.2 注释规范

```typescript
/**
 * 函数/方法注释（JSDoc格式）
 * @param {类型} 参数名 - 参数说明
 * @returns {类型} 返回值说明
 * @throws {Error} 异常说明
 * @example 使用示例
 */

// 单行注释：说明复杂逻辑

/* 多行注释：说明大段逻辑 */
```

### 3.3 文件组织

```typescript
// 1. 导入语句（按类型分组）
import React from 'react';                    // 第三方库
import { View, Text } from 'react-native';    // UI框架
import { useRouter } from 'expo-router';       // 路由
import { COLORS } from '../config/theme';      // 项目配置
import UserService from '../services/user';    // 业务服务

// 2. 类型定义
interface Props {
  userId: string;
  onComplete: () => void;
}

// 3. 常量定义
const MAX_ITEMS = 10;

// 4. 组件/函数实现
export default function UserList({ userId, onComplete }: Props) {
  // ...
}
```

---

## 4. 构建与部署

### 4.1 PC端构建

```bash
cd admin
npm install
npm run build    # 构建生产版本，输出到dist目录
```

### 4.2 移动端构建

```bash
cd expo-domes
npm install
expo build:android    # 构建Android APK
expo build:ios        # 构建iOS IPA
```

### 4.3 后端构建

```bash
cd oa_server
npm install
npx prisma generate   # 生成Prisma客户端
npm run build         # 编译TypeScript
npm start             # 启动服务
```

---

**文档版本**：v1.0  
**更新日期**：2026年5月  
**维护人员**：开发组
